services:
  - type: web
    name: aurum-signals
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python aurum_bot.py
    envVars:
      - key: PORT
        value: 10000
